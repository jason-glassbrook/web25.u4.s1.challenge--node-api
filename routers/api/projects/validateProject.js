/***********************************************************
  validateProject
***********************************************************/

const {
  respondWithError,
  requireRequestConforms,
} = require ('../../../middleware')

const validateProject = () => (ri, ro, next) => {
  next ()
}

/**************************************/

module.exports = validateProject
